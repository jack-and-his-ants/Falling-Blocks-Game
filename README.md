# Falling-Blocks-Game
A simple ASCII falling-block game written in C using ncurses inspired by Tetris. Play directly in your terminal.

##  How to Build

Make sure you have `gcc` and `ncurses` installed:

```bash
sudo apt install gcc libncurses-dev
make
make run
